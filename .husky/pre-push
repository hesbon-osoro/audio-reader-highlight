#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run checks but don't block pushes
echo "Running pre-push checks..."
yarn run typecheck && yarn run lint && yarn run format:check || echo "Pre-push checks failed but allowing push to proceed"

#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged for staged files (format + eslint) - but don't block commits
echo "Running pre-commit checks..."
pnpm lint-staged || npx lint-staged || yarn lint-staged || npx --no-install lint-staged || echo "Pre-commit checks failed but allowing commit to proceed"
